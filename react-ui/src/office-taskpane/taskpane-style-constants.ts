import { additionalRightMarginPx, leftMarginPx } from "../common/results-display/ResultsArea";
import { scrollbarWidth } from "../standalone/scrollbar-width";

export const leftMargin = `${leftMarginPx}px`;
export const rightMargin = `${scrollbarWidth + additionalRightMarginPx}px`;
