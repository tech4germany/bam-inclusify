server {
    listen       80;
    listen  [::]:80;
    server_name  localhost;

    location / {
        root   /srv/inclusify/frontend;
        index  index.html index.htm;
    }

    location /v2/ {
        proxy_pass http://localhost:8081;
    }
}
